[project]
name = "repository-health-bot"
version = "0.1.0"
description = "Local-only repository health checks with CI report artifacts."
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
